export enum ELocalStorageKeys {
  TOKEN = "token",
  POSITION = "position",
  MESSAGES = "messages",
  TOPIC = "topic",
}

export enum EDiscussionMods {
  WORD_MASTERY = "word-mastery",
  QA = "qa",
}

export const IS_DESKTOP_DEFAULT = window.innerWidth > 1024;
